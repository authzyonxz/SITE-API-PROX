#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../katex/cli.js" "$@"
else
  exec node  "$basedir/../../../katex/cli.js" "$@"
fi
# cmd-shim-target=/home/ubuntu/SITE-API-PROX/node_modules/.pnpm/streamdown@1.6.11_@types+mdast@4.0.4_micromark-util-types@2.0.2_micromark@4.0.2_react@19.2.6/node_modules/katex/cli.js
